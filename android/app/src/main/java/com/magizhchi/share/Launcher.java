package com.magizhchi.share;

/**
 * Plain entry-point marker class.
 * The real launch activity is LoginActivity (declared as MAIN/LAUNCHER in AndroidManifest.xml).
 * This file exists to satisfy build tooling that expects a Launcher class in some setups.
 */
public class Launcher {
    // No-op — see LoginActivity
}
