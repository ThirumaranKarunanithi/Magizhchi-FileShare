# Copy this file to .env and fill in your values
# The Vite dev server proxies /api and /ws to VITE_API_URL automatically,
# so you don't need this for local development — it's needed for production builds.

VITE_API_URL=http://localhost:8080
